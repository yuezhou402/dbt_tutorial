version: 2

models:
  - name: stg_payments
    description: Staged payment data from stripe.
    columns:
      - name: payment_id
        description: primary key for payments
        tests:
          - unique
          - not_null